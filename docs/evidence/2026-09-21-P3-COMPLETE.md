# DEV ROOM P3 — COMPLETE

Date: 2026-09-21
Phase: P3 — Reusable PC/SP browser QA gate
Result: COMPLETE

## Completion contract

P3 requires a reusable browser QA path for:

- PC and SP/mobile responsive checks
- navigation / critical-route verification
- console and runtime error capture
- screenshot / visual evidence
- deterministic regression verification
- no ConoHa/VPS dependency

All requirements are satisfied.

## Canonical implementation

- Shared assertion/regression core: `qa/browser-qa-core.mjs`
- Existing managed Steel runner: `qa/browser-qa.mjs`
- Default zero-extra-cost local browser runner: `qa/browser-qa-local.mjs`
- Browser-independent contract test: `qa/selftest.mjs`
- Real Chromium/CDP self-test: `qa/chromium-selftest.mjs`
- Example reusable config: `qa/example.config.json`
- Package commands: `package.json`

Relevant commits:

- `245f1c293af4ea244dfb04eafa6f3070e01e741f` — add VPS-free local Chromium browser gate
- `773b31465e086c76cb88ca3d6e0dab8289855cb4` — add real Chromium P3 self-test
- `d5b3fcbe12ee32610d8ab874e3fe3994874db923` — wire reusable browser QA commands
- `7d437f1fe0aa607e95a7a19adb88ffcf6c89d485` — make VPS-free browser QA path canonical

## Real browser execution proof

The committed Chromium runner and self-test were executed against an inline deterministic HTML fixture using installed `/usr/bin/chromium` in the automation execution environment.

Observed result:

```json
{
  "ok": true,
  "provider": "local-chromium-cdp",
  "checks": [
    "pc_1440x900",
    "sp_393x852",
    "document_load",
    "console_runtime_clean",
    "required_selector",
    "no_horizontal_overflow",
    "screenshots_sha256"
  ]
}
```

The underlying browser report contained exactly 2 test cases, 2 PASS, 0 FAIL. The PC viewport was 1440x900, the SP viewport was 393x852, required DOM selectors were present, no horizontal overflow was detected, console/runtime error arrays were empty, and both screenshots produced 64-character SHA-256 hashes.

## Regression proof

`qa/selftest.mjs` deterministically verifies:

- responsive PC/SP profile definitions
- navigation URL assertion logic
- horizontal-overflow detection
- broken-image detection
- console error detection
- page/runtime exception detection
- required-selector detection
- screenshot-hash regression detection
- title regression detection

The runtime runner additionally supports per-route baselines for title, screenshot SHA-256, document-width delta, and required selectors.

## Current infrastructure evidence inspected in this run

- GitHub repository `NORIZO0201/norizo-dev-room` is the canonical code/state/evidence repository.
- Supabase projects `omnw-production`, `craft-nihon-wine`, and `sayaka-kitchen` were observed as `ACTIVE_HEALTHY`; no schema mutation was required for P3.
- Vercel project `norizo-dev-room` had existing `READY` production deployments. No Preview and no Production deployment was created during P3.

## Provider and cost policy

The canonical default is local installed Chrome/Chromium through the Chrome DevTools Protocol. Steel remains available only when an account key is already present and managed browser execution is useful. No paid browser session is required to use or validate the P3 gate.

## VPS exclusion

ConoHa remains retired. P3 does not inspect, start, recreate, or depend on ConoHa or any VPS.

## Requirement mapping

| Requirement | Evidence | Result |
| --- | --- | --- |
| PC/SP responsive checks | `DEFAULT_PROFILES` + real Chromium self-test | PASS |
| Navigation / route checks | `Page.navigate`, HTTP status capture, URL assertion | PASS |
| Console/runtime errors | `Runtime.consoleAPICalled` + `Runtime.exceptionThrown` | PASS |
| Screenshots / visual evidence | `Page.captureScreenshot` + SHA-256 | PASS |
| Regression verification | shared baseline comparison + deterministic self-test | PASS |
| No VPS dependency | local CDP or managed Steel only | PASS |

P3 is complete and the fixed phase order may advance to P4.

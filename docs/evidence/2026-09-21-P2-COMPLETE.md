# P2 Completion Evidence — OMNW Discovery/Master + Consumer Development Support

Date: 2026-09-21 JST

## Result

**P2: COMPLETE**

OMNW Discovery/Master remains a backend/data-engine lane, Consumer development remains a separate surface, and the retired Harvest pipeline is absent from the inspected canonical code/data surfaces.

## GitHub evidence

Canonical OMNW production branch inspected at `NORIZO0201/oh-my-nihon-wine` main commit:

- `1a926caace6fe12621966a52e9593f6f721c68ba`

The OMNW product constitution explicitly separates Consumer UI from Backend/Data Engine. The project README states that Consumer auth/storage/recognition/Friends are adapter boundaries and the current development implementation can remain local/device-only. `omnw-lab/` is an internal Backend/Data Engine development surface and is not exposed in production.

A separate Consumer Recognition Core development branch was observed:

- branch: `agent/recognition-core-m0-m4`
- head: `eb4c4221c46545573807f1e7cf0a1c363ba80860`
- head message: `M2: add deterministic evidence scoring and alias matching`

This is evidence that Consumer M0→M4 work can progress independently from the Discovery tables. This DEV ROOM run did not modify that branch.

GitHub code search for the retired `Harvest` pipeline in the OMNW canonical main branch returned no matching pipeline implementation.

## Supabase evidence — omnw-production

Project: `alqnnnkuvdpxkqxdqted` (`omnw-production`), status `ACTIVE_HEALTHY`.

Discovery/data-engine tables observed:
- `discovery_sources` — 202 rows
- `discovery_runs` — 302 rows
- `discovery_candidates` — 2,986 rows
- `wineries` — 423 rows
- `wines` — 2,290 rows

Consumer-owned tables observed:
- `user_profiles` — 1 row
- `wine_experiences` — 10 rows
- `unknown_wine_submissions` — 10 rows

Schema-name audit returned no table/view with `harvest` in its name.

Foreign-key audit showed no Consumer table (`user_profiles`, `wine_experiences`, `unknown_wine_submissions`) depending directly on `discovery_sources`, `discovery_runs`, or `discovery_candidates`.

Current OMNW Edge Functions inspected:
- `sync-winery-sources`
- `omnw-official-source-collector`
- `omnw-resident-step`

No Harvest-named Edge Function exists. `omnw-resident-step` explicitly allowlists only `discovery` and `master` lanes; Consumer is excluded from that worker surface.

## Boundary protection added in DEV ROOM

- `contracts/omnw-boundary.json` — commit `3115bd6d5c5cc7b865a0d4f1240dfca33860d70c`
  - Harvest is forbidden.
  - Consumer direct writes to Discovery/Master tables are forbidden.
  - Consumer M0→M4 development may use local adapters, fixtures, or Consumer-owned tables without mutating Discovery data.
- `contracts/omnw-p2-audit.sql` — commit `154193c1a80c35e69c689475e9055e4a8991d3c8`
  - SELECT-only deterministic audit for Harvest absence and Consumer→Discovery FK contamination.

## Deployment safety

This DEV ROOM run did not create an OMNW Vercel Preview or Production deployment.

An already-existing branch deployment for `agent/recognition-core-m0-m4` was observed in Vercel (`dpl_6mCwHxiGXhoUC7dV5Lry3Wb4VnJi`, branch head `eb4c422...`); it was not initiated by this DEV ROOM run. The DEV ROOM policy remains: do not create a Preview unless NORIZO explicitly asks to see one.

## P2 completion rule satisfied

- Discovery/Master separate from Consumer: PASS
- retired Harvest pipeline absent from inspected canonical code/schema/functions: PASS
- Consumer M0→M4 can develop without direct Discovery mutation: PASS
- deterministic boundary/audit contract persisted: PASS
- no Preview/Production initiated by this run: PASS

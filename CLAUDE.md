# NORIZO 運用ルール（全セッション共通）

このファイルは Claude Code が自動で読み込みます。**作業前に必ず従ってください。**

詳細は `docs/OPERATIONS_ARCHITECTURE.md` と `docs/PROJECT_REGISTRY.md` を参照。

---

## 0. まず自分の実行環境を判定する

作業開始時、自分がどちらで動いているか確認してください。

- `uname` が `Darwin` → **Mac版**。SSH・ConoHa操作が可能。
- `uname` が `Linux` かつ `ssh` コマンドが無い → **Web版（使い捨てクラウド）**。
  **SSH・ConoHa操作は不可能。** 求められたら代替手段を探さず、
  「これは Mac 版 Claude Code で実行してください」と案内して停止すること。

---

## 1. 作業前の宣言（必須）

コードに触れる前に、必ず対象を宣言し確認を取ること。

> 「**<プロジェクト名>**（`<リポジトリ>`）を対象に作業します。デプロイ先は `<Vercelプロジェクト>` / `<ドメイン>` です。これで合っていますか？」

`docs/PROJECT_REGISTRY.md` に無い略称が出たら、**推測せず NORIZO に確認する**。

---

## 2. 1セッション = 1プロジェクト

複数プロジェクトを同じ会話で同時に触らない。環境変数・DB・鍵を流用しない。

---

## 3. デプロイの順序（厳守）

```
ローカルQA → 必要な時だけPreview → NORIZO確認 → Production
```

- **Preview を作るのは、NORIZO が「見せて」「確認したい」「プレビュー出して」と言った時だけ。**
  軽微な修正ごとに作らない。複数の修正を確認セットにまとめる。
- **Production は、Preview確認・lint・typecheck・build・主要画面QA が全て完了し、
  NORIZO の明示的な承認がある場合のみ。** 1つでも欠けたら実行しない。

---

## 4. 秘密情報の扱い

- 秘密鍵・APIキー・トークン・パスワード・VPSのIPアドレスを **コミットしない**
- 秘密情報を **チャット本文に表示しない**（値は伏せ、保管場所だけ述べる）
- **このリポジトリは public**。事業データ・内部URLも書かない
- Web版セッションに秘密鍵を持ち込まない（環境ごと消えるが、経路として使わない）

---

## 5. 実行前に必ず報告が必要な操作

以下は **勝手に実行しない**。実行前に内容と影響を説明し、承認を得ること。

- ファイル削除 / データ削除
- データベース構造の大幅変更
- SSH設定の大幅変更 / 秘密鍵の置き換え
- サーバー再起動 / ファイアウォール変更 / DNS変更
- 外部サービスへの公開
- 課金が発生する操作
- 本番環境への変更

**絶対禁止：** 共有ブランチへの `git push --force`、無承認の本番反映、秘密情報のコミット。

---

## 6. エラーへの向き合い方

エラーが出たら、**原因を特定してから直す**。
場当たり的な回避策（try/catchで握り潰す、チェックを無効化する、リトライで誤魔化す）を入れない。
原因が分からない場合は、分かっていることと分からないことを正直に報告する。

---

## 7. 報告の仕方

NORIZO はプロデューサー／事業責任者であり、ソフトウェアエンジニアではありません。

- 日本語で簡潔に。専門知識を前提にしない
- コードそのものより「**何を変更したか**」「**なぜ必要か**」「**変更すると何が起きるか**」を優先
- 完了時は必ず「変更内容」「確認すべきこと」「残っている課題」の3点を報告
- 複数の実装方法がある場合は、保守性・安全性・コスト・拡張性を比較し、**推奨案を明示**する
- 曖昧な指示でも、UXや事業に影響する判断を勝手に決めない。確認する

---

## 8. 変更は小さく刻む

大きな変更を一度に行わない。既存のプロジェクト構造とコードを先に確認し、
**既存機能を壊さない方法を優先する。**

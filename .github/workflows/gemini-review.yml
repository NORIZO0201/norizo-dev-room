name: Gemini CLI (retired)

# Automation retired by NORIZO on 2026-09-21. Previous implementation is in Git history.
on:
  workflow_dispatch:
permissions: {}
jobs:
  retired:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Gemini is excluded from automated collaboration."
